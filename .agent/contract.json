{
  "schema_version": 1,
  "work_issue": 2854,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Let structural admission recognise a do-block migration that rewrites an existing function by reading pg_get_functiondef into a variable and EXECUTEing that same variable, so PR #2818 can draw a reviewer, without widening the shared collision inventory or loosening any existing refusal.",
  "base_sha": "20a6b1c5115e792f994305a8b8f81327b2319dd7",
  "dispatcher": "Albert Hazan (current chat, repo-maintenance session)",
  "worker": "claude-repo-session-2854",
  "branch": "claude/admission-do-block-execute",
  "worktree": "C:/repos/shared-db/.claude/worktrees/sweet-taussig-0796f5",
  "allowed_paths": [
    "scripts/orchestrator-flow/admission.mjs",
    "scripts/orchestrator-flow/admission-outcome.test.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "scripts/orchestrator-flow/admission.mjs",
    "scripts/orchestrator-flow/admission-outcome.test.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No change to inventoryDdlVerbs or dispatchObjectKeys in scripts/check-pr-object-collisions.mjs.",
    "No removal or loosening of the filename-only, truncated-patch, or unmodelled-DDL refusals.",
    "No blanket pass for do-blocks: a do-block that only writes rows must still be refused.",
    "No shared database reads or writes, no migration file, no migration author lane.",
    "No edits to PR #2818 or issue #2744."
  ],
  "required_checks": [
    "node --test scripts/orchestrator-flow/admission-outcome.test.mjs",
    "node --test scripts/check-dispatch-collision.test.mjs",
    "node --test scripts/manage-migration-author-lanes.test.mjs",
    "git diff --check"
  ],
  "assumptions": [
    "The recogniser reports `function schema.name`, the same object key dispatchObjectKeys yields for CREATE OR REPLACE FUNCTION, so declared-writes matching in the lane manager is unchanged.",
    "The test 'guarded merge derives and admits its one live linked structural issue' fails on unchanged main 20a6b1c5 because its claim expiry was hard-coded to 2026-09-13 and has now passed; moving that expiry relative to the current clock repairs it without changing what it asserts."
  ],
  "stop_conditions": [
    "Stop if recognising the rewrite requires changing the shared collision inventory.",
    "Stop if any existing refusal test stops refusing."
  ]
}
