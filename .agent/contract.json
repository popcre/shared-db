{
  "schema_version": 1,
  "generation": 1,
  "work_issue": 3249,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Raise the nightly ColdLion Landing Sync job ceiling from 90 to 150 minutes so the prepack and production-detail refreshes fit: live measurement on 2026-09-18 put worst case at ~116 minutes and the run was cancelled mid-prod-detail-refresh.",
  "base_sha": "57a2c400ef3af9530c9e99a6723b76ab2e5e2922",
  "dispatcher": "owner task instruction continuation for shared-db #3180 (source-data session; #3249 is the repo-maintenance lane for this diff)",
  "worker": "ZCode GLM 5.3 session executing shared-db issue #3180",
  "branch": "claude/issue-3180-sync-job-budget",
  "worktree": "C:/repos/shared-db/.claude/worktrees/prod-detail-3180",
  "allowed_paths": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/coldlion-landing-sync.yml"
  ],
  "file_writes": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/coldlion-landing-sync.yml"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "Never weaken or remove the stall-killing timeout (#1266); only raise it to a measured ceiling.",
    "Never change the workflow's triggers, secrets handling, target declarations, offline-gate-before-write ordering, or concurrency serialization."
  ],
  "required_checks": [
    "node --test tools/coldlion-landing-prod-details.test.mjs tools/coldlion-landing-prepack.test.mjs"
  ],
  "assumptions": [
    "Live measurement 2026-09-18, dispatch run 35303647480, cancelled at the 90-minute job timeout while the prod-detail refresh was mid-flight: setup+offline gate ~4 min, history ~2, masters ~4, prepack ~43, prod-detail ~1.9s/key (1,263 keys in ~39 min before cancellation) with up to 2,000 recent orders selected.",
    "150 minutes = ~116 worst case + margin, and the concurrency group (cancel-in-progress false) still serializes the lane."
  ],
  "stop_conditions": [
    "Stop and report if the raised budget is still exceeded by a real run.",
    "Stop and report on any gate refusal; never weaken a guard to proceed."
  ]
}
