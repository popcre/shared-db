{
  "schema_version": 1,
  "generation": 1,
  "work_issue": 2863,
  "work_type": "structural",
  "route": "shared-db-migration",
  "goal": "Author the ColdLion landing tables coldlion.prepack_detail and coldlion.prod_detail at reserved migration version 20260916001944, with a documented grain proof per endpoint, a contract test file, and the mandatory production verification sidecar pinned in PREVIEW_PRODUCER_PATHS. Structure only: no row loads, no preview or production apply.",
  "base_sha": "1dcf196ea9b506ec14feb55e8465b53d9faf1706",
  "dispatcher": "Claude shared-db orchestrator (claim 3006, marker issue 3004, 2026-09-15)",
  "worker": "Claude Opus 5 issue-2863 ColdLion detail landing author",
  "branch": "claude/issue-2863-coldlion-detail-landing",
  "worktree": "C:/repos/shared-db/.claude/worktrees/issue-2863",
  "allowed_paths": [
    "supabase/migrations/20260916001944_coldlion_prepack_and_prod_detail_landing.sql",
    "supabase/tests/coldlion_prepack_prod_detail_landing_contracts.sql",
    "docs/coldlion-unit-5b-grain-proof-20260915.md",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    "scripts/production-verification-sidecars/20260916001944.json",
    "scripts/production_business_risk_gate.py",
    "scripts/production_catalog_verification.py",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "supabase/migrations/20260916001944_coldlion_prepack_and_prod_detail_landing.sql",
    "supabase/tests/coldlion_prepack_prod_detail_landing_contracts.sql",
    "docs/coldlion-unit-5b-grain-proof-20260915.md",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    "scripts/production-verification-sidecars/20260916001944.json",
    "scripts/production_business_risk_gate.py",
    "scripts/production_catalog_verification.py",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [
    "coldlion.prepack_detail",
    "coldlion.prod_detail"
  ],
  "prohibited_actions": [
    "No apply to preview or production; Supabase MCP stays read-only",
    "No merge of this or any other pull request",
    "No use of any migration version other than the reserved 20260916001944",
    "No database object outside the claimed coldlion.prepack_detail and coldlion.prod_detail",
    "No row loads into the new tables",
    "No work in another worktree or on another branch",
    "No weakening of any existing check or guard"
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "node scripts/check-production-verification-sidecars.mjs --base origin/main",
    "node --test scripts/check-sql.test.mjs",
    "python -m pytest -q scripts/test_production_business_risk_gate.py scripts/test_check_production_verification_sidecars.py",
    "python -m pytest -q scripts/test_production_catalog_verification.py"
  ],
  "assumptions": [
    "Migration version 20260916001944 is reserved for this claim and must be used exactly",
    "Both ColdLion endpoints are enumerable rather than paged, so a live grain census is possible from a sampled key set",
    "A fully static migration still requires a production verification sidecar, and every sidecar path must be individually pinned in PREVIEW_PRODUCER_PATHS",
    "Adding a catalog_contract requires a CATALOG_CONTRACTS entry plus a refreshed detector_source_sha256 in the reviewed throughput-guard baseline",
    "The governed verification connects as supabase_read_only_user, so every catalog predicate must be evaluated on that connection before it is trusted"
  ],
  "stop_conditions": [
    "Stop and report if the grain census shows a repeated group for either endpoint, rather than labelling a guessed key as natural",
    "Stop if the ColdLion API cannot be reached, and say so explicitly rather than guessing a key",
    "Stop if the work would require writing any database object outside the claim",
    "Stop if a verification predicate cannot be shown to fail on a deliberately wrong expectation"
  ]
}
