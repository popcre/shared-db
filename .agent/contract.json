{
  "schema_version": 1,
  "generation": 5,
  "work_issue": 3232,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Enforce the owner's 2026-09-17 ruling (\"I never want GLM reviewing GLM code\") in the reviewer draw: the glm rows declare orchestratorEngine:'glm', a zcode engine entry lets a ZCode orchestrator declare itself honestly in the routing contract, and reviewersForOrchestrator maps the declared engine through ENGINE_REVIEWER_EXCLUSION so a zcode orchestrator never draws glm while codex and claude orchestrations behave exactly as before.",
  "base_sha": "579f8fe0c88fdd288cfa9c0eb9453318ae1cf7ec",
  "dispatcher": "ZCode (GLM 5.3) repo-maintenance session completing issue #3232 (self-published per docs/agents/agent-work-contract-repo-maintenance-evidence.md: the implementation predated publication, and the completion report states that provenance plainly)",
  "worker": "ZCode (GLM 5.3) session, issue #3232",
  "branch": "zcode/issue-3232-glm-reviewer-independence",
  "worktree": "C:/repos/shared-db/.claude/worktrees/zcode-3232-glm-independence",
  "allowed_paths": [
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/lib/orchestrator-routing.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/lib/orchestrator-routing.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "do not add ZCode as a reviewer: the same owner ruling permanently rejected that",
    "do not change any reviewer's registration, retirement, quarantine, or rotation membership",
    "do not weaken the fail-closed unreadable-engine refusal in reviewersForOrchestrator",
    "do not import lib/orchestrator-routing.mjs into the lane manager: that would make every routing edit a global evidence invalidator",
    "do not author a migration or touch any database object"
  ],
  "required_checks": [
    "node --test scripts/manage-migration-author-lanes.test.mjs",
    "node --test scripts/check-orchestrator-marker.test.mjs"
  ],
  "assumptions": [
    "ZCode session ids are shaped sess_<uuid> (ZCODE_SESSION_ID), matching the id pattern ai-blocker-watch already resumes",
    "the marker resolver keeps rejecting engines outside the ENGINES vocabulary, so the raw-string fallback in reviewersForOrchestrator is unreachable from a declared marker",
    "no reviewer registration or rotation membership changes: ACTIVE/RETIRED/QUARANTINED are name-for-name identical to main"
  ],
  "stop_conditions": [
    "stop and re-declare if the change must touch any file outside allowed_paths",
    "stop if the routing engine vocabulary and ENGINE_REVIEWER_EXCLUSION drift apart (the lane test suite asserts key-for-key agreement)",
    "stop if a marker declaring no engine stops refusing reviewer assignment"
  ]
}
