{
  "schema_version": 1,
  "generation": 2,
  "work_issue": 3198,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Make the migration author lease check accept a grant/revoke on a table the same migration file creates when the claim declares that table, restoring PR #3190 (claim #3189), without relaxing the declaration requirement for grants on pre-existing relations.",
  "base_sha": "4fec003d67bd036ef0e0b265b28b8c7de0aceaa8",
  "dispatcher": "Albert Hazan chat request to fix the lease check refusing grants on created tables",
  "worker": "ZCode (GLM-5.3) #3198 session",
  "branch": "claude/fix-lease-grant-created-table",
  "worktree": "C:/repos/shared-db/.claude/worktrees/fix-lease-grant-table-3191-090754",
  "allowed_paths": ["scripts/check-migration-pr-lease.mjs","scripts/check-migration-pr-lease.test.mjs",".agent/contract.json",".agent/completion.json"],
  "file_writes": ["scripts/check-migration-pr-lease.mjs","scripts/check-migration-pr-lease.test.mjs",".agent/contract.json",".agent/completion.json"],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": ["No database write or structure change of any kind", "No change to check-pr-object-collisions.mjs grant dual-keying"],
  "required_checks": ["node --test scripts/check-migration-pr-lease.test.mjs", "node --test scripts/check-pr-object-collisions.test.mjs scripts/check-dispatch-collision.test.mjs"],
  "assumptions": ["A migration cannot create a table and a view with the same qualified name, so a view key on a table created in the same file can only come from grant/revoke dual-keying", "Claims cannot be amended after creation, so the view half must be covered lease-side", "Generation 2: refreshed by merge onto main b2633a33; the implementation diff is unchanged"],
  "stop_conditions": ["Stop if the fix would relax the view declaration requirement for a relation the migration does not create", "Stop if the collision guard's dual-keying would need to change"]
}
