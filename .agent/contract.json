{
  "schema_version": 1,
  "work_issue": 2876,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Repair the post-apply catalog verifier row-count query so PostgreSQL orders composite rows by their name field instead of applying a JSON operator to a record, while preserving full catalog verification.",
  "base_sha": "c23dcdcf928f352aab2a144f52182cea4554bf17",
  "dispatcher": "successor orchestrator marker 2858",
  "worker": "subagent:issue-2866-author assigned issue 2876",
  "branch": "codex/issue-2876-catalog-row-order",
  "worktree": "C:/Users/ahazan/.codex/worktrees/issue-2876-catalog-row-order/shared-db",
  "allowed_paths": [
    "scripts/production_catalog_verification.py",
    "scripts/test_production_catalog_verification.py",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "scripts/production_catalog_verification.py",
    "scripts/test_production_catalog_verification.py",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No database read or write.",
    "No migration, preview, production dispatch, or merge.",
    "No weakening, skipping, or replacing any catalog verification or enforcement path.",
    "No write outside the verifier, its regression test, the reviewed detector-hash baseline, and the evidence pair."
  ],
  "required_checks": [
    "failing regression for the SQLSTATE 42883 composite-record query shape",
    "focused row-count formatter tests",
    "full production catalog-verification test suite",
    "ai-task-gates check --before review",
    "git diff --check"
  ],
  "assumptions": [
    "The derived-table alias x is a PostgreSQL composite record, so deterministic ordering must use x.name or an explicit JSON conversion rather than x->>'name'."
  ],
  "stop_conditions": [
    "Stop if the repair requires any database access or workflow replay.",
    "Stop if full catalog verification cannot be preserved."
  ],
  "generation": 3
}
