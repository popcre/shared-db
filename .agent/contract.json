{
  "schema_version": 1,
  "work_issue": 2851,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Grant the automatic-production-promotion job the minimum permission (contents: write) to create its db-coordination admission commit object and ref, so automatic and manual production promotion stop failing with HTTP 403, without giving that job any database credential or weakening its ENGINEER ACTION REQUIRED fail-closed behaviour.",
  "base_sha": "62d31d7138b0d598217521d2550e3c3e82adb376",
  "dispatcher": "Albert Hazan (owner, repo-maintenance request 2026-09-13)",
  "worker": "claude-repo-session-2851",
  "branch": "claude/fix-auto-promotion-contents-write",
  "worktree": "C:/repos/shared-db/.claude/worktrees/cranky-mestorf-ee0f23",
  "allowed_paths": [
    ".github/workflows/shared-supabase-migrations.yml",
    "scripts/test_production_owner_decision_evidence.py",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    ".github/workflows/shared-supabase-migrations.yml",
    "scripts/test_production_owner_decision_evidence.py",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No permission change on any job other than automatic-production-promotion; the top-level and production-apply-review contents: read stay.",
    "No database secret added to automatic-production-promotion.",
    "No removal, weakening or bypass of ENGINEER ACTION REQUIRED fail-closed logic.",
    "No shared database reads or writes, no migration file, no author lane claim.",
    "No re-dispatch of issue 2482's production promotion."
  ],
  "required_checks": [
    "python -m unittest scripts.test_production_owner_decision_evidence",
    "python -m unittest scripts.test_production_migration_guard",
    "git diff --check",
    "node --test scripts/manage-migration-author-lanes.test.mjs"
  ],
  "assumptions": [
    "contents: write covers both Git data API writes the admission path makes: POST git/commits and POST/PATCH git/refs. No other scope is needed.",
    "Generation 6 refreshes onto main 62d31d71 and restores main's evidence pair before sealing, so the reported implementation head has exactly the workflow and focused test in its own diff. Main's PR #2855 reviewer-lifecycle repair is inherited unchanged."
  ],
  "stop_conditions": [
    "Stop if the admission step needs any permission beyond contents: write.",
    "Stop if the fix would require giving this job a database credential.",
    "Stop and report the exact refusal if a governed tool refuses, rather than working around it."
  ],
  "generation": 6
}
