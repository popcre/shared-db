{
  "schema_version": 1,
  "generation": 1,
  "work_issue": 3193,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Make the shared production migration guard name the ledger it actually read, so a preview-job refusal says 'already applied on preview' and points to the historical-recovery lane, without changing any check.",
  "base_sha": "23d6142e079a805d86a7e5a0cf4324897c732bcd",
  "dispatcher": "Albert Hazan chat request to fix the #2794 preview re-rehearsal deadlock",
  "worker": "Claude Opus 5 #3193 session",
  "branch": "claude/issue-rehearse-applied-evidence-only",
  "worktree": "C:/repos/shared-db/.claude/worktrees/agent-a1916a8ff61864114",
  "allowed_paths": [
    "scripts/production_migration_guard.py",
    "scripts/test_production_migration_guard.py",
    ".github/workflows/shared-supabase-migrations.yml",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "scripts/production_migration_guard.py",
    "scripts/test_production_migration_guard.py",
    ".github/workflows/shared-supabase-migrations.yml",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No database write or structure change of any kind",
    "Do not let any already-applied version pass validate_candidates on any ledger"
  ],
  "required_checks": [
    "python -m unittest scripts/test_production_migration_guard.py"
  ],
  "assumptions": [
    "The existing historical-recovery lane already recovers a post-merge rehearsal that ran at the authoring merge commit (AGENTS.md section 6), so no new evidence-only mode is needed"
  ],
  "stop_conditions": [
    "Stop if the change would let an applied version be applied again or loosen any producer pin"
  ]
}
