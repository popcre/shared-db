{
  "schema_version": 1,
  "work_issue": 2865,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Repair the nightly ColdLion Landing Sync, which failed 2026-09-12 and 2026-09-13 with 'spawnSync psql EPIPE' while saving the master snapshot: pass the landing loader's SQL to psql as a private temp file instead of stdin so a large load cannot break the pipe and any real database error is surfaced.",
  "base_sha": "bce5f2e005c67f7a8196105f98d48bf746e612b4",
  "dispatcher": "Albert Hazan (owner) via coordinator: owner-authorized repo-maintenance repair of the ColdLion Landing Sync nightly",
  "worker": "shared-db repo-maintenance session in worktree coldlion-api-storage-6c5a79",
  "branch": "claude/coldlion-landing-psql-file-input",
  "worktree": "C:/repos/shared-db/.claude/worktrees/coldlion-api-storage-6c5a79",
  "allowed_paths": [
    "tools/coldlion-landing/lib/db.mjs",
    "tools/coldlion-landing-masters.test.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "tools/coldlion-landing/lib/db.mjs",
    "tools/coldlion-landing-masters.test.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No database structure change of any kind.",
    "Do not weaken, disable, or bypass the sync job, ON_ERROR_STOP, the spawn timeout, or client-fault classification.",
    "Do not hide the error: a real psql failure must still fail the job with its database message."
  ],
  "required_checks": [
    "node --test tools/coldlion-landing-history.test.mjs tools/coldlion-landing-masters.test.mjs"
  ],
  "assumptions": [
    "EPIPE from spawnSync with piped input means psql stopped reading before the script was fully written; Node then reports only the spawn error and drops status and stderr.",
    "The history half of the job is healthy: both failed runs reported 0 outstanding windows through 2026-09-07, and the current week is never loaded by design."
  ],
  "stop_conditions": [
    "Stop if the fix would require a database structure change.",
    "Stop if a check can only be made green by weakening a guard."
  ],
  "generation": 1
}
