{
  "schema_version": 1,
  "generation": 1,
  "work_issue": 3245,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Fold case in the /prepackDetail loader key-coverage accounting and collect each vendor row once when two harvest spellings of one drifted key are both asked, so the #3179 backfill can complete.",
  "base_sha": "f048cca84c40edaae7814545e205e07bd76ebb51",
  "dispatcher": "delegated source-data session continuing issue #3179 production phase",
  "worker": "Claude GLM 5.3 source-data session, issue #3179",
  "branch": "claude/issue-3179-prepack-coverage-case",
  "worktree": "C:/repos/shared-db/.claude/worktrees/prepack-cover-3245",
  "allowed_paths": [
    ".agent/completion.json",
    ".agent/contract.json",
    "tools/coldlion-landing-prepack.test.mjs",
    "tools/coldlion-landing/sync-prepack-detail.mjs"
  ],
  "file_writes": [
    ".agent/completion.json",
    ".agent/contract.json",
    "tools/coldlion-landing-prepack.test.mjs",
    "tools/coldlion-landing/sync-prepack-detail.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "Never collapse rows that differ for one grain: identical bytes across two spellings are one row, differing bytes refuse loudly.",
    "Never let rowsFetched under-report: it keeps the raw fetch count so nothing landed is hidden.",
    "Never author a migration or change any database shape.",
    "Never close issue #3179; the originating session owns closure."
  ],
  "required_checks": [
    "node --test tools/coldlion-landing-prepack.test.mjs"
  ],
  "assumptions": [
    "The harvest holds both spellings of the drifted key as distinct codes, both answering the row spelling (runs 35288752430 and 35294603177 together prove the shape).",
    "rowsFetched reports raw fetched rows while the returned sourceRows are deduplicated; the run notes and the landed counts stay honest about both."
  ],
  "stop_conditions": [
    "Stop and report a blocker if the deduplicated load ever disagrees with the API totals by more than the counted zero-row keys.",
    "Stop on any unresolved guard or reviewer refusal; a new head answering it requires a fresh exact-head review."
  ]
}
