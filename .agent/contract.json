{
  "schema_version": 1,
  "generation": 3,
  "work_issue": 3224,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Pass an explicit --pause-ms 1000 on the daily ColdLion Landing Sync's prepack-detail refresh step so the 2,577-code re-read fits the job's 90-minute budget instead of overrunning it at the loader's 3,000ms default.",
  "base_sha": "ec96eb745697dbfa0a6eb2191efae7ab40bcb158",
  "dispatcher": "delegated source-data session continuing issue #3179 production phase",
  "worker": "Claude GLM 5.3 source-data session, issue #3179",
  "branch": "claude/issue-3179-prepack-refresh-pacing",
  "worktree": "C:/repos/shared-db/.claude/worktrees/prepack-detail-3179",
  "allowed_paths": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/coldlion-landing-sync.yml"
  ],
  "file_writes": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/coldlion-landing-sync.yml"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "Never author a migration or change any database shape; this is a workflow pacing change only.",
    "Never weaken a workflow guard: the offline tests still run before any write, the declared target stays beside every credential, and the missing-secret refusals stay.",
    "Never remove or reorder the history-then-masters-then-prepack-then-prod-details sequence.",
    "Never close issue #3179; the originating session owns closure."
  ],
  "required_checks": [
    "node --test tools/coldlion-landing-prepack.test.mjs"
  ],
  "assumptions": [
    "The live dry-run dispatched after PR #3220 merged measured 2,577 distinct harvested prepack codes and ~3.1s per key at the 3,000ms default pace.",
    "The vendor agreement is serialized one-request-at-a-time; a 1,000ms pause keeps it while landing the full refresh in ~45 minutes beside history+masters' ~8 minutes, inside the 90-minute job budget.",
    "Generation 3 exists because main advanced to ec96eb74 (PR #3230, the sibling /proddetails company_code fix, rewrote the shared .agent pair files); the branch was refreshed by merge (b6800355) and the pacing change plus the coexisting prod-detail step carry over unchanged."
  ],
  "stop_conditions": [
    "Stop and report a blocker if the measured daily refresh plus history, masters and prod-detail would exceed the job timeout at any acceptable pace.",
    "Stop on any unresolved guard or reviewer refusal; a new head answering it requires a fresh exact-head review."
  ]
}
