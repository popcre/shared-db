{
  "allowed_paths": [
    ".agent/contract.json",
    ".agent/completion.json",
    "scripts/check-dispatch-collision.mjs",
    "scripts/check-dispatch-collision.test.mjs",
    "scripts/orchestrator-snapshot.mjs",
    "scripts/orchestrator-snapshot.test.mjs",
    "scripts/check-migration-pr-lease.mjs"
  ],
  "assumptions": [
    "The owner account hit the 5,000 requests/hour GitHub limit twice this week; the fix removes wasted reads and adds no throttle, sleep or longer interval.",
    "scripts/manage-migration-author-lanes.mjs is owned by open PRs #2981 and #2640 and is not touched here.",
    "check-exact-head-approval.mjs is unchanged: its ref names lead with the issue number, so matching-refs cannot be narrowed to one pull request.",
    "Generation 1 already exists for issue 2985; this contract is generation 2, republished after main (e317e92a) was merged so the evidence pair follows the implementation head."
  ],
  "base_sha": "e317e92ae6e28d7df8c16964d5b94b21f14bfcf9",
  "branch": "claude/github-request-waste-fix",
  "db_reads": [],
  "db_writes": [],
  "dispatcher": "repository-maintenance session (not orchestrator work, AGENTS.md rule 16)",
  "file_writes": [
    ".agent/contract.json",
    ".agent/completion.json",
    "scripts/check-dispatch-collision.mjs",
    "scripts/check-dispatch-collision.test.mjs",
    "scripts/orchestrator-snapshot.mjs",
    "scripts/orchestrator-snapshot.test.mjs",
    "scripts/check-migration-pr-lease.mjs"
  ],
  "generation": 2,
  "goal": "Remove wasted GitHub API reads in check-dispatch-collision, orchestrator-snapshot and check-migration-pr-lease while keeping every safety proof and output identical.",
  "prohibited_actions": [
    "No database, migration, preview, production, claim or marker mutation.",
    "No edits to scripts/manage-migration-author-lanes.mjs.",
    "No throttles, sleeps or longer polling intervals."
  ],
  "required_checks": [
    "git diff --check",
    "node --test --test-reporter=tap scripts/check-dispatch-collision.test.mjs scripts/orchestrator-snapshot.test.mjs scripts/check-migration-pr-lease.test.mjs scripts/check-exact-head-approval.test.mjs",
    "node scripts/check-throughput-truth-audit.mjs"
  ],
  "route": "repo-maintenance",
  "schema_version": 1,
  "stop_conditions": [
    "Stop if reducing requests would require weakening the changed_files completeness proof or dropping any comment, claim or file from the evidence read."
  ],
  "work_issue": 2985,
  "work_type": "repo-maintenance",
  "worker": "Claude Opus 5 subagent",
  "worktree": "C:/repos/shared-db/.claude/worktrees/pr2984"
}
